// Main component
export { Sidebar } from './Sidebar';

// Configuration
export { menuItems } from './menu.config';
export type { MenuItem } from './menu.config';

// Sub-components (for advanced customization)
export { SidebarLogo, SidebarMenuItem, SidebarFooter } from './components';

// Hooks (for reuse in other components)
export { useSidebarState } from './hooks';
